@page
@model CitizenServices.Pages.NearbySchoolsModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schools[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schools[0].AcceptedAges)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Schools)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AcceptedAges)
                </td>
            </tr>
        }
    </tbody>
</table>